<p>For every pomodoro I complete I donate Rs. 5 to <%= link_to 'Oscar India','http://www.oscar-india.org' %></p>

<table  class="table table-striped table-bordered table-condensed">
  <tr>
    <th>Pomodoros Completed</th>
    <th colspan="4">Money Donated (in rupees) </th>
  </tr>
  <tr>
  	<th>Week</th>
    <th>Week</th>
    <th>Month</th>
    <th>Year</th>
    <th>Lifetime</th>
  </tr>
  <tr>
    <td>16</td>
    <td>80</td>
    <td>215</td>
    <td>215</td>
    <td>215</td>
  </tr>   
</table>

<div id="barChart"></div>

<div class="center" ><strong>Pomodoros completed last week</strong></div>
  <!--[if IE]><script src="excanvas.js"></script><![endif]-->
  <script src="html5-canvas-bar-graph.js"></script>
  <script>(function () {
  
    function createCanvas(divName) {
      
      var div = document.getElementById(divName);
      var canvas = document.createElement('canvas');
      div.appendChild(canvas);
      if (typeof G_vmlCanvasManager != 'undefined') {
        canvas = G_vmlCanvasManager.initElement(canvas);
      } 
      var ctx = canvas.getContext("2d");
      return ctx;
    }
    
    var ctx = createCanvas("barChart");
    
    var graph = new BarGraph(ctx);
    graph.maxValue = 10;
    graph.margin = 5;
    graph.colors = ["#CE0000", "#EF2323", "#D20202", "#A70000", "#850000", "#740000", "#530000"];
    graph.xAxisLabelArr = ["Mon","Tue","Wed","Thurs","Fri", "Sat", "Sun"];
    graph.update([4,2, 3, 5, 0, 0, 2]);

  }());</script>

  

